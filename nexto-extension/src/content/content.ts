/**
 * Content script — placeholder for in-page integrations (smart queue, capture, etc.).
 * Built as IIFE for Chrome (see vite.content.config.ts).
 */
console.log('[Nexto] content script ready', window.location.href);
